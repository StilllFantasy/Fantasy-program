#include <stdio.h>
int main()
{
    char c;
    scanf("%c", &c);
    while (c != '.')
    {
        if (c == '(')
        {
        }
        else if (c == ')')
        {
        }
        else
        {
        }
        scanf("%c", &c);
    }
}